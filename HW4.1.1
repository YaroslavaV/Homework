/**
 * The program displays an array, odd elements of the array and their number.
 * 
 * creator of Yaroslava 
 */

package com.gmail.thepowerofyar;

import java.util.Arrays;

public class Main {

	public static void main(String[] args) {

		int[] m = new int[] { 0, 5, 2, 4, 7, 1, 3, 19 };
		int s = 0;
		System.out.println(Arrays.toString(m));

		for (int i = 0; i < m.length; i++) {
			if (m[i] % 2 != 0) {
				++s;
				System.out.print(m[i] + " ");

			}

		}
		System.out.println("Количество нечетных элементов " + s + " ");

	}
}
